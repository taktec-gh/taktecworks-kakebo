/**
 * ログイン画面に出す文言。
 *
 * src/lib/auth.ts ではなくここに置いてあるのは、パスキーのボタン
 * （Client Component）からも同じ文字列を読むため。auth.ts は jose に依存するので
 * クライアント側から import させたくない。auth.ts はここから再 export する。
 */

/**
 * ログイン失敗時に画面へ出すメッセージ。
 *
 * 「未登録のパスキー」「署名不正」「試行しすぎ」等を推測させない単一の文言にする。
 * パスキーログインは失敗理由を問わずこれを返す。
 * （公開版ではパスワードログインを廃止したため、パスワードに触れる文言にしない）
 */
export const LOGIN_ERROR_MESSAGE = "ログインできませんでした。もう一度お試しください。";
