/**
 * ログイン画面に出す文言。
 *
 * src/lib/auth.ts ではなくここに置いてあるのは、パスキーのボタン
 * （Client Component）からも同じ文字列を読むため。auth.ts は jose に依存するので
 * クライアント側から import させたくない。auth.ts はここから再 export する。
 */

/**
 * ログイン失敗時に画面へ出すメッセージ。
 *
 * 「パスワードが未設定」「パスキーが必要」「試行しすぎ」等を推測させない単一の文言にする。
 * パスワードログインもパスキーログインも、失敗理由を問わずこれを返す。
 */
export const LOGIN_ERROR_MESSAGE = "ログインできませんでした。パスワードを確認してください。";
